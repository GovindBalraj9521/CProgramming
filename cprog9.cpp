//To check whether the three numbers are equal or not
#include<stdio.h>
int main()
{
	int x,y,z;
	printf("Enter the values");
	scanf("%d%d%d",&x,&y,&z);
	if(x==y && y==z)
	   printf("Equal");
	else
	   printf("Not equal");
return 0;	   
	      
}
